---
label: Projects
hide_body: true
fields:
- name: layout
  type: text
  config:
    required: false
  label: Layout
  hidden: false
  default: projects
- name: title
  type: text
  config:
    required: false
  label: Title
  description: Title of the project
- name: desc
  type: text
  config:
    required: true
  label: Description
  description: Meta description of the page
- name: order
  type: number
  label: Order
  description: Order in the list
  default: '1'
  required: true
  config:
    min: 
    max: 
    step: '1'
    required: true
- name: cover
  type: file
  config:
    maxSize: 250
  label: Cover
- name: typeds
  type: list
  config:
    use_select: false
    min: 
    max: 
  label: Typed
  description: Words typed in the cover
- name: sections
  type: field_group_list
  fields:
  - name: section_text
    type: blocks
    label: Section Text
    template_types:
    - section-projects
    config:
      min: 
      max: 
    description: Sections with text
  config:
    min: 
    max: 
    labelField: 
  label: Sections
- name: section_pictures
  type: field_group_list
  fields:
  - name: pictures
    type: blocks
    label: Pictures
    template_types:
    - section-pictures
    config:
      min: 
      max: 
  config:
    min: 
    max: 
    labelField: 
  label: Section Pictures
- name: next_project
  type: text
  config:
    required: true
  label: Next project
  description: Next project of the list. If this project is the last one link to the
    first one. Only small caps no space (use - instead)
pages:
- _projects/project-full.md
- _projects/title.md
